overwrite: true
schema: "http://localhost:8080/v1/graphql"
documents: null
generates:
  src/utils/generated/types.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
